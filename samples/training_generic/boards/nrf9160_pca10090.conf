# Enable the sensor driver subsystem, and BME280 sensor support.
CONFIG_SENSOR=y
CONFIG_BME280=y

# Turn on SPI driver support.
CONFIG_SPI=y

# Enable SPI1. This line is needed for NCS v1.2. It will be determined
# entirely by enabling spi1 in the devicetree in a future release.
CONFIG_SPI_1=y
